export const SAMPLE_SKILL_MD = `---
Name: My Deploy Skill
descrption: deploys the app
disable_model_invocation: true
context: fork
---

Deploy the application properly.

Make sure everything is working correctly before deploying.
Handle any errors that come up as necessary.

You should probably run the tests first if appropriate.

The deployment process involves several important steps that need to be
followed carefully to ensure that the application is deployed correctly
and that all the necessary checks are performed before and after the
deployment to verify that everything is working as expected in the
production environment.

When deploying, keep things simple and clean.
Make the deployment process robust and maintainable.

Use good practices for deployment etc...
`
